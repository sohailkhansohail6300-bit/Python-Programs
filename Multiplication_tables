#Here user will enter the number and the program will print the multiplication table of that number.

while True:
    num = input('Enter number or(q to quit): ')
    if num=='q':
        break
    num1=int(num)
    for i in range(1,11):
        print(f'{num1} X {i} = {num1*i}')
